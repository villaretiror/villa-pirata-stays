---
trigger: always_on
---

# 🚨 REGLA DE ORO: PRESERVACIÓN DEL CHAT ENGINE
- EL CHAT YA ESTÁ VIVO Y OPERATIVO. Queda terminantemente PROHIBIDO modificar, renombrar o mover archivos dentro de `/api/chat.ts`, `/api/send.ts` o la carpeta `/constants/`.
- Cualquier mejora en la UI o en el Contrato Digital debe ser ADITIVA. No se puede alterar la lógica de respuesta ni el flujo de datos de `VILLA_KNOWLEDGE`.
- Los archivos `.js` en los imports de las API Routes deben mantenerse así para garantizar la compatibilidad con el runtime de Vercel.